<%- include('../partials/head') %><%- include('../partials/header') %>
<main class="page">
  <section class="page-hero page-hero--divisions">
    <span class="eyebrow"><%= t.pages.divisionsEyebrow %></span>
    <h1><%= t.pages.divisionsTitle %></h1>
    <p><%= t.pages.divisionsSubtitle %></p>
  </section>
  <section class="cards-grid section">
    <% divisions.forEach((division) => { %>
      <article class="division-card division-theme division-theme-<%= division.id %>">
        <img src="<%= asset(division.image) %>" alt="<%= division.title %>" loading="lazy" decoding="async">
        <div class="card-body">
          <h3 class="accent-<%= division.color %>"><%= division.name %></h3>
          <strong><%= division.subtitle %></strong>
          <p><%= division.description %></p>
          <a href="<%= url(pathFor('divisioni', { id: division.id })) %>"><%= t.common.detail %></a>
        </div>
      </article>
    <% }) %>
  </section>
</main>
<%- include('../partials/footer') %>
